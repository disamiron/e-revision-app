export enum ReducerSections {
  USERS = '[USERS DATA]',
  LOCATION = '[LOCATION DATA]',
  REVISION = '[REVISION DATA]',
  SHOP = '[SHOP DATA]',
}

export enum ReducerStatuses {
  INIT = 'INIT',
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
  LOADING = 'LOADING',
}
