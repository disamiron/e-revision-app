export enum Scope {
  User = 'USER',
}

export enum Roles {
  admin = 'admin',
  moderator = 'moderator',
  entrepreneur = 'entrepreneur',
  user = 'user',
}

export enum MenuButtonsType {
  Volume = 'volume',
  Vibration = 'vibration',
  TextToSpeech = 'textToSpeech',
}

export enum RevisionStatus {
  started = 'started',
  notStarted = 'not-started',
  completed = 'completed',
}

export enum ColorMap {
  black = '#000b0b',
  white = '#ffffff',

  lightBluePolls1 = '#bee7e7',
  lightBluePolls2 = '#eff5f5',
  lightBluePollsFocused = '#cceaea',
  lightBlue = '#83dbdb',
  lightBlueFocused = '#65e4e4',
  blue = '#00a8b4',

  grey = '#e4e7e7',
  greyFocused = '#616d6d',
  greyText1 = '#798686',
  greyText2 = '#303635',

  red = '#ff5555',

  redAlert = '#d60000',
  greenAlert = '#609f62',
}

export enum ButtonColorMap {
  primary = 'primary',
  accent = 'accent',
  warn = 'warn',
}
